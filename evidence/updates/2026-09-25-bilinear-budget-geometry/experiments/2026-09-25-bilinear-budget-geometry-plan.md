# Paired saved-score geometry across bilinear training budgets

Declared before new diagnostic execution. Campaign `bilinear-budget-geometry-v1`
is descriptive posthoc analysis of already observed validation predictions. It
is not a model-quality screen or a new evaluation on unseen examples. The
completed 8000-update replication has been handed to the publication task.

## Question and authenticated evidence

Which boundary failures disappeared between 2000 and 8000 residual updates, and
what remains across the three frozen parent feature spaces? Distinguish relative
ranking from absolute zero-boundary set membership without fitting thresholds.

Authenticate the accepted 8000 replication chain:
- Aggregate `77cc2ee2ebf4dc1f8671e71833712302160190c85477eb6e0dd0b5494bb3cde6`.
- Independent audit `49fc5d7edc7763870ac0e9310295385d8e6a3af6e67d3cc6b5a9f0e44bc2ae77`.
- Owner decision `01a9faefb90ee3d3e7525c0d5191d41741131843dc79ea2b44d860c5c8e21d5e`.

Authenticate the accepted 2000 replication chain:
- Aggregate `338fb6b7ea08128952ce9abc02b3506639e712a9cff6ce63468ed2e7980f79a0`.
- Independent audit `ddbecaceabdacc0808f9b4f122e0309aa3e233fe76b0d75694e31833a40083a8`.
- Owner decision `62a8dcbb7cbe5d038984f9d57aecacec4b0ba612ba55c2a9f6fe335977304978`.

Resolve each historical 1729 and fresh 1730/1731 child through its budget's
accepted summary/audit/decision chain, authenticating every referenced child
report and matching checkpoint identity without loading checkpoint payloads.
Require completed per-seed audits and accepted aggregate evidence; do not accept
a matching filename as proof. Historical 1729 remains development-selected at
both budgets. Fresh 1730/1731 observations reuse the same 222 validation questions.

Also authenticate the existing 2000 saved-error diagnostic:
- Summary `1a38b93a27a77eb597637798be4f8b24ef858c546d6a7c25aed8ba8f85a99606`.
- Audit `1b10b77db77ebb79b5f1327bcc1538331eb876216c424036a516a0ac04cdba96`.
- Decision `4f2d624341cf03d4af8e7c6976135a1b6766071899e745ea4071f9f1cf04e652`.

Reuse compatible scientific helpers from `scripts/diagnose_bilinear_errors.py`,
SHA `66093bfbd06c8622b61535eeaffd0b09886c0cee05ec9eae71292a6e85925789`.
Preserve its executed bytes and all earlier evidence. A new versioned wrapper
binds the new input chains and paired-budget analysis. Reconstructed 2000
per-seed reductions must equal the historical diagnostic's authenticated rows,
totals and groups. Existing 33-checkpoint historical metadata remains historical;
this diagnostic starts after the current 37-checkpoint inventory.

## Fixed reductions and comparisons

Align index, subject, dimension, group, prompt IDs and expected set IDs across
all six reports. Require 222 ordered queries and 1025 product columns 1024..2048.
Reconstruct each original nonself z>0 selection in ascending ID order and
reconcile selected IDs, exactness, FP/FN and strict separation with its audited
report. Require finite, exactly representable FP32 scores, valid truth masks,
self exclusion and nonempty true/false classes. No tolerance or rounding.

For each query let a=min true score, b=max false nonself score and g=a-b. Use
the existing four exclusive categories in order: exact at zero; nonexact and
strictly separated with missing members only; nonexact and strictly separated
with extra members only; nonexact with overlapping or tied ranges. Preserve
deterministic lowest-ID witnesses for extrema and all wrong membership IDs/scores.

For each seed and each group publish the full 4x4 transition matrix from 2000
to 8000 categories, exact-answer gains/losses and separation gains/losses.
Reconcile exact gains/losses to the already accepted 8000 comparisons. Publish
all 222 paired per-query a,b,g values and differences, not selected examples.
Differences are descriptive score changes within each parent, not calibrated
confidence or causal explanations. No cross-parent score averaging.

For each budget report per-seed/group categories and shared-query failure-seed
lists/overlap counts. Fresh-two and all-three totals retain their distinct roles;
444/666 are model-query observations, not independent sample sizes. Include
pooled category transitions by summing counts, without pooled threshold intervals.

Include per-seed and per-seed/group shared-interval existence at each budget:
A=min_query(a), B=max_query(b), exists iff B<A. For a hypothetical z>t rule the
label-informed interval is [B,A), with deterministic lowest query-index witnesses.
These are descriptive bounds: do not select a t, generate alternate predictions
or present interval existence as a deployable rule or performance estimate.

## Execution, verification and publication

Read only authenticated JSON reports, plans and source/test provenance. No
checkpoint payloads, Torch, GPU, graph DB, training examples, protected data,
model forward, optimizer update or new checkpoint. Exactly one primary reduction
invocation is intended after source/plan/tests are frozen. Preserve any execution
failure; a repair requires a separate versioned record, never silent overwrite.

Synthetic CPU tests must cover boundary signs and ties, FP/FN replay, extrema
witnesses, finite FP32 constraints, query/column drift, cross-budget alignment,
transition totals/gains/losses, historical-reduction equality, accepted versus
incomplete evidence chains, production receipt schemas and immutable outputs.
Bind tests and all executed helper dependencies to a completed test receipt.
Preflight may authenticate schemas/identities but must not execute new reductions.

A separate stdlib auditor independently reconstructs all reductions, transitions,
intervals and reconciliations from authenticated saved reports. It must not import
primary scientific arithmetic; compatible independently authored old audit helpers
may be reused only with exact hash binding. Freeze and synthetically test its
actual schema contract before use. Schema coordination must avoid circular hashes.

Retain immutable plan/source/helper/test/input snapshots, per-seed and summary
results, observed terminal completion, independent audit and owner acceptance.
Each published file must be <=512 KiB; split per-seed output deterministically if
needed, with ordered full-coverage checks. Include all per-query reductions.

Update registry, research/dev logs, a teaching lesson and PLM-findings; notify
the dedicated Luna Max publisher immediately after a completed reviewed handoff.
Notify the canonical-paper owner: this is unpromoted descriptive evidence, not
a change to the retained model. Current final-checkpoint count remains 37, and
no inference/evaluator quality gate/default changes. No threshold tuning, extra
training, prediction routing or protected evaluation is authorized by this plan.
