# Research update — 2026-09-25

This addition preserves the original evidence snapshot and adds newly available
records. `snapshot.json` maps every copied input to its source path and SHA256;
`SHA256SUMS.txt` covers this update's files. Source copies are byte-identical,
including their original status statements and limitations.

- `model-versions.md` and the inventory report register 23 final checkpoints.
- `experiments/` includes the completed native concurrency probe, the pending
  shape-aware verification contract and the future learned-retrieval hypothesis.
- `raw/` contains the completed native probe's full summary, bound by its
  portable report. This is recorded execution evidence, not a new replay here.
- `learning/` contains lesson 29 on numerical verification and evidence reuse.
- `scripts/` and `tests/` preserve the inventory method and its regression tests.
  They belong to the source repository layout and require its local run artifacts;
  copying them does not make this findings repository a standalone model runtime.

No model weights, protected-test outputs or raw dataset databases are included.
Links within copied source lessons may point to earlier lessons or experiment
files in the original snapshot instead of this dated subdirectory. Synthesis
papers provide direct links to their cited evidence.
