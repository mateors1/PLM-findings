# 2026-09-25: hardest-boundary margin screen

Completed, independently audited negative result: a fixed margin 1.0 and loss
coefficient 0.1 reduced fresh-control exact answers from 192/222 to 6/222 at
seed 1729. The variant stops before seeds 1730/1731. Both checkpoints are
retained; no default was promoted and the protected test remains unused.

The copied plan, portable report, full primary summary, independent audit and
separate owner decision bind the comparison. The 25-run inventory supersedes
neither the historical 23-run snapshot nor its recorded limitations.

The diagnosis is explicitly post hoc, descriptive and based on saved vectors;
it does not establish the causal optimization mechanism or retune the gate.

Files listed in snapshot.json are byte-identical source copies. Source/config
archives and effective training configs are included. Large checkpoint payloads
and raw prediction vectors remain in ignored local source-run directories;
their hashes are bound, but this addition is not a durable checkpoint backup or
an independently replayed GPU training result. CPU prerequisite results are
owner-attested because the original suite stdout was not archived.

SHA256SUMS.txt covers every addition file except itself. Existing dated evidence
and its checksum manifests are preserved. The papers contain interpretation;
the copied evidence remains unchanged.
