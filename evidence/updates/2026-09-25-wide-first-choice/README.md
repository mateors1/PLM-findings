# 2026-09-25: eight-first-choice search

The declared three-seed offline experiment passes independent audit and its
fixed quality gate: exact answers improve 569→603/666 and macro F1
0.966896→0.983493. There are 35 gains and one loss. Four additional branches
roughly double observed generation time. No serving default, new checkpoint
or protected-test result is established. The inventory remains 27.

snapshot.json lists byte-identical source copies. Frozen plan/runner/runtime
archives, compact reports, independent audit, separate decision, focused tests
and their receipts are included. The large per-seed neural/baseline reports and
checkpoint payloads remain local, hash-bound dependencies; this compact addition
is not a self-contained neural reproduction or a durable model backup. Copied
notes can link outside this addition into the source repository. Earlier copies
remain unchanged. SHA256SUMS.txt covers all new files except itself.

The current user instruction keeps active research within Pokémon. The copied
external-data review is explicitly parked; further work on another dataset
requires an explicit user request.
