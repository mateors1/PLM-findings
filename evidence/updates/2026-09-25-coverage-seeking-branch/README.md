# 2026-09-25: one coverage-seeking generation branch

This addition records a fixed inference screen over the accepted eight-source
Pokemon baseline. One positive-head product outside existing coverage starts
an extra greedy path; its original set and eight unions extend the old pool.
Labels do not choose anchors or candidates. The independent audit and separate
owner decision distinguish evidence acceptance from the fixed quality gate.
The fixed gate rejects the policy: exact answers fall 603 to 600 despite
higher pooled F1. See the portable report and paper 15 for complete outcomes.

All historical head vectors and forced-first rank-one controls are fresh and
exact at original B8/last6 shapes; the eight baseline paths are reused. Inactive
rows retain numerical padding, excluded from candidate selection. Timing is
incremental and descriptive. set_scoring_seconds includes postprocessing,
scoring and per-query analysis, not isolated scorer latency.

snapshot.json lists byte-identical copies. Frozen plan, runner, helper, runtime
archives, focused tests, independent audit, owner decision and 666 compact query
diagnostics are included. Full raw per-seed neural reports, original upstream
reports and weight payloads remain local hash-bound dependencies. This is not
a durable checkpoint archive or self-contained neural reproduction bundle.
Checkpoint inventory remains 27; no training, protected-test use or default
change. Historical copied evidence retains its bytes. SHA256SUMS.txt covers
all new files except itself. Copied notes may link to the source repository.
