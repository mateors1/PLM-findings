# 2026-09-25: eight-source pair intersections and pure-operation bounds

The independent audit accepts the saved-output reconstruction. The prediction
variant fails its fixed gate: pooled exact answers improve603→605/666, but
seed1729 F1 decreases. No policy/default is promoted. The final-checkpoint
inventory remains27, with no training, neural execution or protected-test use.

Pure unions add no exact availability. Either exhaustive pure family offers610
exact candidates; old-first ties and unchanged scores limit selected exactness
to at most608. These label-assisted bounds are not predictive results. Active
research remains within Pokémon.

snapshot.json lists byte-identical source copies. The frozen plan/scripts,
compact reports, all666 per-query diagnostics, independent audit, separate
decision and focused-test receipts are included. Full raw64-slot reports,
upstream neural reports and checkpoint payloads remain local hash-bound
dependencies. This is not a self-contained neural reproduction bundle or a
durable model backup. Copied notes may link into the separate source repository.
Historical evidence copies remain unchanged. SHA256SUMS.txt covers all new
files except itself.
